{
  "name": "clawdslist",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "turbo run db:generate --filter=@clawdslist/db",
    "db:push": "turbo run db:push --filter=@clawdslist/db",
    "db:seed": "turbo run db:seed --filter=@clawdslist/db"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.2.0"
}
